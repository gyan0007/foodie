<% provide(:title, 'Restaurants') %>
<h1>Restaurants</h1>

<%= will_paginate %>

<ul class="restaurants">
  <%= render @restaurants %>
</ul>

<%= will_paginate %>
<br style = “line-height:6;”><br>
<% if logged_in? && current_user.admin?%>
  <%= link_to "Add New Restaurant", restaurants_new_path, class: "btn btn-lg btn-primary" %>
<% end %>
